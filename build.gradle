apply plugin: 'java'
apply plugin: 'idea'

jar {
    baseName = 'cleaner'
    version = '0.0.2-RELEASE'
}

dependencies {
    compile fileTree(dir: '../../lib/JavaApi770', include: '*.jar')

}

task wrapper(type: Wrapper) {
    gradleVersion = '2.12'
}